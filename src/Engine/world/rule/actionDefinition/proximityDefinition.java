package Engine.world.rule.actionDefinition;

import Engine.InvalidValue;
import Engine.generated2.PRDAction;

public class proximityDefinition extends actionDefinition{
    public proximityDefinition(PRDAction action, String ruleName) throws InvalidValue {
        super(action, ruleName);
    }
}
