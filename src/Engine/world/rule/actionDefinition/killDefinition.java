package Engine.world.rule.actionDefinition;

import Engine.InvalidValue;
import Engine.generated2.PRDAction;

public class killDefinition extends actionDefinition{
    public killDefinition(PRDAction action, String ruleName) throws InvalidValue {
        super(action, ruleName);
    }
}
